<Window x:Class="QuickMusic3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuickMusic3"
        xmlns:viewmodel="clr-namespace:QuickMusic3.MVVM.ViewModel"
        mc:Ignorable="d"
        Height="650" Width="1200">
    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="20,5,10,5">
                <TextBlock Text="{Binding Path=Player.CurrentTime}"/>
                <Slider Maximum="{Binding Path=Player.TotalTime, Converter={StaticResource TimeConverter}}"
                        Value="{Binding Path=Player.CurrentTime, Converter={StaticResource TimeConverter}}"/>
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                        <Button Width="42" Height="42" Margin="4">
                            <Image Source="/Resources/play.png"
                                   Margin="1"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Button Width="42" Height="42" Margin="4">
                            <Image Source="/Resources/repeat_all.png"
                                   Margin="2"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Button Width="42" Height="42" Margin="4">
                            <Image Source="/Resources/shuffle.png"
                                   Margin="1"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Button Width="42" Height="42" Margin="4"
                                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                            <Image Source="/Resources/volume.png"
                                   Margin="1"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Slider Width="100"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Width="42" Height="42" Margin="4">
                            <Image Source="/Resources/previous.png"
                                   Margin="1"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Button Width="42" Height="42" Margin="4">
                            <Image Source="/Resources/next.png"
                                   Margin="1"
                                   SnapsToDevicePixels="True"/>
                        </Button>
                        <Button Width="84" Height="42" Margin="4"
                                Content="Browse"
                                FontSize="16"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
